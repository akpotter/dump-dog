Use in the following way:

SetUnhandledExceptionFilter(YoelUnhandledExceptionFilter);


Expected "l_WatchDog.exe" in c:\ (in some dir - look at the source)

Use "MiniDumpReader" and after that "DumpTextParser" to analyze it.
(Or use windbg)



